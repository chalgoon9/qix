# Android Qix Panic

This folder hosts the Android project under `qixpanic/`.

- Open `qixpanic` in Android Studio and run `app` on a device/emulator.
- Use drag or on-screen D-Pad to steer. Pause button at top-right.
- Claim 75% to clear; tap to proceed between levels.
